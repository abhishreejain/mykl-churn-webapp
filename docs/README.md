# MYKL Churn Webapp Package

This folder is a separate website-ready package scaffold.

It contains:

- `frontend/`
- `backend/`
- `docs/`

This step prepares structure and runtime packaging only. UI and backend API implementation are intentionally deferred.

Runtime source constraint:

- uses original runtime sources from the main project
- does not use `client_handover/scripts`
