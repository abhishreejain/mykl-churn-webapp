# DASHBOARD_CONTRACT

Date: 2026-05-22  
Status: Active contract for all website work from this step onward.  
Supersedes generic preview-page direction.

## Runtime Source-of-Truth (Mandatory)

Website backend must use original main-project runtime only:

1. `scripts/score_input_file.py`
2. `scripts/add_potential_to_scored_file.py`
3. `src/churn_model/`
4. `artifacts/production/`

Must not use:

- `client_handover/scripts/*`

## Header

- `MYKL CHURN PREDICTOR`

## Page Layout

Two-panel dashboard layout:

1. Left control panel
2. Right main workspace panel

## Left Control Panel Contract

Must contain:

1. app title / branding
2. selected file summary
3. Generate button
4. status panel
5. download button area
   - hidden until processing succeeds

## Right Workspace Contract

Before processing:

- show upload guidance
- show accepted input format (`.xlsx`, `.xls`)

After processing:

- show dashboard view

## Dashboard View Contract

### Part 1: Summary graphs/cards using operational priority buckets

Buckets:

- Red:
  - High risk x High potential
  - High risk x Medium potential
  - Medium risk x High potential
- Orange:
  - Medium risk x Medium potential
  - Medium risk x Low potential
  - High risk x Low potential
- Green:
  - Low risk x High potential
  - Low risk x Medium potential

### Part 2: Drill-down filters and influencer list

Must include:

1. state selector
2. risk buttons
3. potential buttons
4. influencer list for selected bracket and state

## Download Contract

After backend completes both steps successfully:

- show button to download final enriched output generated by `add_potential_to_scored_file.py`

## Mapping Contract (Critical)

Potential Level (`High` / `Medium` / `Low`) must be derived from `Potential Band` through explicit config.

Config location:

- `mykl_churn_webapp/frontend/ui_bucket_config.json`

Rules:

1. Mapping must be explicit and reviewable.
2. Mapping must not be silently embedded in UI code.
3. Any missing combination must be handled explicitly (error/other), never silently dropped.

## Visual Direction Contract (for upcoming UI redesign step)

- Color palette must use shades of blue.
- Avoid brown-dominant UI theme from earlier iteration.

This file defines behavior and structure only; it does not perform UI redesign by itself.
